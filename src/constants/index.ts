import path from "path";

export const FOLDER_PATH_FULL_IMAGE = `${path.resolve(
  __dirname,
  "../../Images/full"
)}`;
export const FOLDER_PATH_THUMB_IMAGE = `${path.resolve(
  __dirname,
  "../../Images/thumb"
)}`;
